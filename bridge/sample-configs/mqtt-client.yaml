modules:
  - type: stdout
  - type: timestamp
    config:
      name: TimestampExample
      update_interval: 4
  - type: mqtt
    config:
      publish_events:
        timestamp: fancontrolbridge/test/output
      subscribe_topics:
        - fancontrolbridge/test/input
      host: mqtt.local.lan
